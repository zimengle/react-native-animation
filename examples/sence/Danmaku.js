import React, {Component} from 'react';
import AnimationView from '../../dist/animation-view'
import {Text, Navigator} from 'react-native';

export default class Modal extends Component {
    render() {
        return (
            <Text>this is Modal</Text>
        );
    }
}